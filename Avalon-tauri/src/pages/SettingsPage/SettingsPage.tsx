import { PageContainer, Card } from '../../components/ui';

export function SettingsPage() {
  return (
    <PageContainer title="Settings" description="应用配置，接入后端 config 模块。">
      <Card variant="sunken" description="页面内容待搭建。" />
    </PageContainer>
  );
}
