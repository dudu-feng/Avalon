import { PageContainer, Card } from '../../components/ui';

export function LibraryPage() {
  return (
    <PageContainer title="Library" description="资料库与知识节点，接入后端数据能力。">
      <Card variant="sunken" description="页面内容待搭建。" />
    </PageContainer>
  );
}
